<div class="card">
  <div class="card-block">
    <%= link_to article.title, article_path(article), class: 'btn btn-link hoang'%>

    <% if !showButtonView %>
        <%= link_to 'Delete',
                    article_path(article),
                    method: :delete,
                    data: { confirm: 'Are you sure you want to delete this article?' },
                    class: 'btn btn-danger article-details'%>
        <%= link_to 'Edit', edit_article_path(article), class: 'btn btn-secondary article-details'%>
    <%end%>

    <p class="tiny-label"><%= article.details_string %></p>
    <% if article.feature_image %>
      <img src="<%= article.feature_image %>" class="img-fluid">
    <% end %>
    <p class="card-text body"><%= markdown.render(article.body).html_safe %></p>

    <% if article.tag_list.count > 0 %>
      <p class="card-text tags tiny-label">
        Tags:
        <%= render "tags", article: article, custom_class: 'btn btn-info' %>
      </p>
    <% end %>

  </div>
</div>
